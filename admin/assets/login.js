const form=document.querySelector('#login-form');const error=document.querySelector('#login-error');document.querySelector('#toggle-password').addEventListener('click',()=>{const input=document.querySelector('#password');input.type=input.type==='password'?'text':'password';document.querySelector('#toggle-password').textContent=input.type==='password'?'Show':'Hide'});document.querySelector('#forgot').addEventListener('click',()=>{error.textContent='Password reset requires an administrator-configured email provider.'});form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';const button=form.querySelector('button[type="submit"]');button.disabled=true;try{const response=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.querySelector('#email').value,password:document.querySelector('#password').value})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to sign in.');sessionStorage.setItem('northstar_csrf',data.csrfToken);window.location.assign(data.redirect)}catch(loginError){error.textContent=loginError.message}finally{button.disabled=false}});
